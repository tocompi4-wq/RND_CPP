# ----------- Librerias -----------------
import numpy as np
import matplotlib.pyplot as plt
import random # Para la generación de números aleatorios


# Leer los datos generados por C++
try:
    df = pd.read_csv('6to_punto.csv')
except FileNotFoundError:
    print("Error: El archivo 'datos_aleatorios.csv' no existe. Corre primero el codigo en C++.")
    exit()

# Graficar LCA (Datos de C++)
plt.scatter(df['x_lca'], df['y_lca'], s=2, color='purple', alpha=0.6)
plt.title("LCA 48-bits (Desde C++)")


plt.tight_layout()
plt.show()
